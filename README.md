# MicroChat
Modern, scalable chat application built with a microservices architecture.

### How to Setup:
1) git clone https://github.com/Asembly/MicroChat.git
2) rename .example.env files and configure for you
3) docker compose up --build -d
4) go to the /client dir, and enter **npm run build && npm start**
